## 아키텍처

### Gradle 구조

`:trading-core`(매매 실행 도메인 — trading/matching/broker/account/privacy/marketcalendar), `:shared`(sharedkernel/platform — outbound-zero 공용 어휘+인프라 leaf, Stage 3 Task 1에서 `:trading-core`로부터 기계적 분리), 루트 `:api`(그 외 전부, bootJar 산출) 세 서브프로젝트. 루트와 `:trading-core` 모두 `:shared`를 단방향 `implementation` 의존하고, 루트가 `:trading-core`를 단방향 `implementation` 의존한다(`shared ← trading-core ← api`, `shared ← api` DAG). 배포는 루트의 `app.jar` 하나 — 이 분리는 컴파일 경계일 뿐 런타임 분리가 아니다(런타임 프로세스 분리는 `kista-api`/`kista-scheduler` 2-role이 이미 별도로 담당). `trading-core/build.gradle.kts`·`shared/build.gradle.kts` 둘 다 JUnit Platform 버전 정합을 위해 `org.springframework.boot` 플러그인을 전체 적용하되 `bootJar` 태스크는 `enabled = false`로 비활성화(산출물 없음, root만 실제 fat jar 생성). `com.kista.support`(테스트 헬퍼)·`application-test.yml`은 루트 `src/testFixtures`에 있고 `java-test-fixtures` 플러그인(루트)으로 `testFixtures(project(":"))` 경유해 trading-core 테스트가 소비한다. `db/migration`은 이동하지 않고 루트 전용으로 남아있어, 이를 상대경로로 읽는 trading-core 테스트를 위해 `trading-core/build.gradle.kts`의 `test` 태스크가 `workingDir = rootProject.projectDir`로 설정돼 있다. `:trading-core→:api` 역방향 의존 금지는 `GradleModuleBoundaryTest`(`src/test/java/com/kista/architecture`)가 컴파일 산출물(`trading-core/build/classes/java/main`) 기준으로 강제 검증하고, `:shared→:trading-core`/`:shared→:api` 역방향 의존 금지도 같은 테스트가 `shared/build/classes/java/main` 기준으로 함께 검증한다 — main 소스 기준이며, trading-core의 테스트 소스는 예외적으로 루트를 `testImplementation(project(":"))`로 역참조한다(user/admin 소유 보안 테스트 스캐폴딩 재사용 목적, 보안 모듈 경계 정리 시 해소 대상).

Hexagonal Architecture (Port & Adapter). **ArchUnit이 빌드 시 레이어 의존성을 강제 검증**한다 (`HexagonalArchitectureTest`). `domain_must_not_depend_on_outer_layers`는 `com.kista..domain..` 전체를 예외 없이 커버한다 — 전략 구현체 Spring 배선은 `CycleStrategyBeanConfig` 팩토리가 전담한다.
클래스·필드 상세는 코드가 SSOT — 아래 맵은 위치·역할·비자명한 규칙만 기록한다 (record aggregate 분리 제약 → constraints.md "Account ↔ Strategy 분리").

Spring Modulith 기반 애그리게이트 모듈 구조다. 레거시 최상위 `com.kista.domain`/`com.kista.application`/`com.kista.adapter` 패키지는 전부 소멸했고, 각 클래스는 아래 모듈 절에 명시된 위치로 이전 완료됐다. 마이그레이션 경위·발견된 모듈 순환·해소 방법의 전체 이력은 `docs/agents/modulith-migration-history.md` 참고 (필요시 Read).

```
DB 스키마 3분리(kista/finance/reference): kista=순수 매매 도메인(계좌·주문·전략·포지션 + trading-core 소유 `user_notify_profile` 복제본), finance=가계부, reference=외부 참조·시장 데이터(FIDA PRIVACY 기준 매매표 포함, 전역 공유·비개인 데이터가 기준). 인증/관리자/로그/알림 성격 테이블(users/user_settings/user_notification_prefs/refresh_tokens/broker_tokens/admin_runtime_settings/audit_logs/app_error_logs/fcm_device_tokens/scheduler_locks)은 플랫폼 공통이라 public 유지. 신규 테이블은 이 기준으로 분류해 Entity에 `@Table(schema=...)` 명시(public도 명시 — search_path 첫 스키마가 kista라 생략 시 validate 실패) — nativeQuery/JdbcTemplate/raw SQL은 DB 유저 search_path(`kista, finance, reference, public`)로 unqualified 이름이 자동 해석되므로 스키마 접두사 불필요

com.kista.sharedkernel/ ← 순수 어휘 패키지. `@ApplicationModule(Type.OPEN)` — outbound reference 0인 값 타입만 담아 다른 모듈을 참조하지 않는다는 전제라 열린 채로 여러 모듈이 자유롭게 소비(enum이 대부분이나 `StrategyCreationSettings`/`StrategyFieldSettings`처럼 자체 검증 로직을 가진 record도 outbound-zero면 포함). 담긴 타입: UserRole/UserStatus/NotificationType + StrategyType/StrategyStatus/StrategyTicker/StrategyCycleSeedType(→ constraints.md "Account ↔ Strategy 분리") + Broker(TOSS/KIS/MOCK) + RecurringMode/StrategyCreationSettings/StrategyFieldSettings(전략 생성 정책 어휘 — 외부 계약에 묶이지 않은 내부 정책값이라 own-type 유지 대신 통합) + OrderDirection/OrderType/OrderStatus/OrderTiming(매수/매도 방향·주문 유형·상태·타이밍 — KIS/Toss wire 매핑은 어댑터 코드가 담당할 뿐 enum 값 집합 자체엔 외부 계약이 실려있지 않아 own-type 정당화가 성립하지 않음, → constraints.md "모듈 경계 own-type") + StrategyDefaults(`DEFAULT_DIVISION_COUNT=20` 상수 — PRIVACY/VR처럼 분할 수 설정이 없는 전략의 고정값, admin/trading/stats/matching 공통 참조). user/account 모듈은 이 패키지를 소비만 하고 소유하지 않는다. `NotificationChannel`은 user 단독 소비라 `com.kista.user.domain.model` 독립 파일 잔류(sharedkernel 미포함). broker의 `BrokerAccountRef`는 sharedkernel.Broker를 직접 참조한다(record 자체는 자격증명 투영 own-type으로 존속). 담긴 값 타입 외 JDK-only 유틸 `TimeZones`(KST 단일 소스)/`AccountNumberMasker`(계좌번호 마스킹 SSOT, Task 8 fix round 1에서 `com.kista.account.domain.model`→이관 — Account 자체에 의존 없는 순수 문자열 유틸이라 own-type 복제 대상이 아니었음)도 함께 둔다. `UsTradeDates`(KST↔US 거래일 변환)는 어댑터 전용이라 `com.kista.platform.time`으로 분리됨 — 아래 `com.kista.platform/` 참고. `UserDeletedEvent(UUID userId)`(원래 `com.kista.user.application.event` 소유)는 trading(cyclePosition/strategyCycle cascade·user_notify_profile 복제본 삭제)·finance(cascade)·account(계좌 cascade) 등 여러 CLOSED 모듈이 구독하는 이벤트라 sharedkernel로 승격됨 — user는 여전히 발행 주체지만 타입 자체는 sharedkernel 소유. `UserNotifyProfileChangedEvent(userId, notificationPrefs, balanceCheckEnabled, active)`도 같은 이유로 sharedkernel 소유 — trading-core가 이 타입을 import해야 하므로 `com.kista.user`에 두면 `:trading-core → :api` 역방향 컴파일 의존이 되살아난다(아래 `com.kista.trading/` user_notify_profile 항목 참고). `TradingReport`(5필드 매매 리포트 — notify `UserNotificationPort.notifyTradingReport()` 시그니처)/`ReturnMetrics`(자산곡선 성과 지표 순수 계산 유틸 — root `HousingBenchmarkComparisonBuilder`와 trading-core `BacktestEngine`/`BacktestService` 공용)/`DailyCandle`(백테스트용 일봉 OHLC 값 타입)은 원래 trading-core 소유였으나, root가 trading-core 정의 타입을 구현/참조할 수 없는 컴파일 경계(`:api → :trading-core` 단방향) 때문에 sharedkernel로 승격됐다 — 세 타입 모두 sharedkernel enum+JDK 타입만 참조해 outbound-zero를 유지한다. `CycleCompletedEvent`/`CycleEndedEvent`/`NewCycleStartedEvent`/`InsufficientBalanceEvent`/`TradingReportReadyEvent`(Task17)도 같은 이유로 승격됐다 — 원래 trading-core `application.event` 소유였고 notify 리스너가 `Account`/`Strategy`/`AccountBalance`/`List<Execution>`을 그대로 실어나르며 `AccountPort.findByIdOrThrow()` 재조회를 안고 있었으나, notify가 실제로 쓰는 필드(`Account.nickname` 1개, `AccountBalance` 2개, `Strategy` 3개)만 스칼라로 narrowing한 뒤 sharedkernel로 옮겨 재조회 자체를 없앴다. `TradeLegSummary`(direction/ticker/quantity/price/amountUsd 5필드)는 `broker.domain.model.Execution`의 notify 전용 narrowing으로 신설된 값 타입 — `TradingReportReadyEvent.executions`가 sharedkernel+JDK 타입만 담도록 `List<Execution>`을 대체한다. `OrderCancelFailedEvent`/`TradingErrorEvent`/`MarketClosedEvent`/`MarketOpenEvent`/`MarketCloseEvent`/`BatchInterruptedEvent`(trading 소유)·`PrivacyAlertRaisedEvent`(privacy 소유)는 Task17 fix round 2에서 마저 승격됐다 — 필드 전부 순수 JDK/enum이라 narrowing 없이 패키지 선언만 교체했고, 유일하게 `BatchInterruptedEvent`만 `UUID accountId`뿐이던 필드에 `String accountNickname`을 추가해 `TradingAlertNotifier.onBatchInterrupted()`의 `AccountPort` 재조회를 없앴다 — 이로써 trading-core `application.event`·privacy `application.event` 두 패키지 모두 완전히 비어 `package-info.java`째로 삭제됐다("event" NamedInterface 소멸, 위 각 모듈 절 참고). `com.kista.sharedkernel.port` 서브패키지에는 예외적으로 순수 인터페이스 3개(`BrokerEnabledPort`/`StrategyCreationPolicyPort`/`HistoricalCandlePort`)가 있다 — root(`RuntimeSettingsService`/`AlpacaIndexPriceAdapter`)가 trading-core(account/trading) 정의 인터페이스를 implements하는 구조는 타입 identity가 필요해 own-type 복제로 대체할 수 없었기 때문이며, 시그니처가 sharedkernel+JDK 타입(`Broker`/`StrategyCreationSettings`/`StrategyType`/`DailyCandle`)만 사용해 outbound-zero 조건은 값 타입과 동일하게 유지된다.

com.kista.platform/  ← 전역 인프라 leaf 모듈. `@ApplicationModule(Type.OPEN)` — sharedkernel과 동일하게 outbound-zero를 증명할 때만 안전한 OPEN이며, `HexagonalArchitectureTest.platform_must_not_depend_on_other_modules`가 이를 강제한다(`com.kista.platform..` 외 `com.kista..`는 전부 금지 — `sharedkernel_must_not_depend_on_other_modules` 미러). Spring/JPA 바인딩이 있어 sharedkernel(순수 JDK)과 분리한다.
  persistence/   ← BaseAuditEntity(`createdAt`+`updatedAt`)/BaseCreatedAtEntity(`createdAt`만)/JpaAuditingConfig(`@EnableJpaAuditing` 단독 선언 — `@SpringBootApplication`에 두면 `@WebMvcTest` BeanCreationException). `@Setter(AccessLevel.PACKAGE)` 범위 주의는 → constraints.md "Lombok @MappedSuperclass 상속 주의"
  crypto/        ← AesCryptoService(AES-256, persistence 경계에서만 사용)/AccountNoHasher(계좌번호 결정론적 HMAC-SHA256 해시 — 전역 중복 체크용)/Sha256(RT 해시·Toss token fingerprint 공용 헬퍼 — 구 com.kista.common, 모듈 경계 재구성 #3으로 이관)
  time/          ← UsTradeDates(KST↔US 거래일 단순 ±1 변환 — 순수 java.time). 사용처는 `HexagonalArchitectureTest.usTradeDates_must_only_be_used_by_allowlisted_adapters`가 4클래스(KisTradingApi/KisPriceApi/TossPriceApi/MarketCalendarPersistenceAdapter)로 강제 — 도메인·서비스·orders persistence 사용 금지. 구 `com.kista.common` → sharedkernel(모듈 경계 재구성 #3) → platform(어댑터 전용이라 "공용 어휘"가 아님)
  scheduling/    ← SchedulerJobRunner(공통 실행 골격 — 시작 시 STARTED·완료 시 COMPLETED·실패 시 FAILED `SchedulerLifecycleEvent` 발행 + 인터럽트 처리. `run(String, Runnable)` / 제네릭 `<T> run(String, Supplier<List<T>>, Action<T>)`)/SchedulerLockService(package-private 분산 락 — `tryRun(lockKey, timeout, task)`; `@ConditionalOnProperty(scheduler.enabled)` 로컬 중복 실행 방지 — Postgres 기반 `scheduler_locks` 테이블, DB 서버 시각 `now()` 기준 `INSERT ... ON CONFLICT ... WHERE lock_until <= now()`이라 다중 인스턴스라도 시계 편차 없이 안전하게 경쟁)/SchedulerLifecycleEvent(jobName, Phase{STARTED,COMPLETED,FAILED}, errorMessage). `SchedulerJobRunner`는 `NotifyPort`를 직접 주입하지 않고 이벤트만 발행하며, notify가 `SchedulerNotifier`(`@TransactionalEventListener(fallbackExecution=true)`)로 구독한다. `RefreshTokenCleanupScheduler`(user) 등 기존 소비자도 이 골격을 재사용. 정확한 실행 시각·락 TTL은 `scheduler-time-table.md` 참고. **`@Scheduled` 사용 프로세스는 `@EnableScheduling` 필수** — 빠지면 스프링이 에러 없이 조용히 무시(컴파일·기동 모두 정상)한다. trading-core 분리(4a) 이후 `TradingApplication`에 이 선언이 누락돼 개장/마감 자동매매 cron을 포함한 모든 `@Scheduled` 스케쥴러가 배포 기간 내내 한 번도 자동 실행되지 않은 사례가 있었다(4b-1에서 발견·수정, `com.kista.trading.TradingApplication`) — 신규 부트 클래스 추가 시 `@Scheduled` 소비자가 하나라도 있으면 반드시 확인

com.kista.matching/  ← Spring Modulith 이전 모듈(CLOSED) — 주문생성 커널(순수 계산). `domain/model`(주문 어휘 enum·`PlannedOrder`·position 값객체·`AccountBalance`) + `domain/strategy`(`CycleOrderStrategy` 계열·`PriceCapPolicy`) 두 패키지가 `"kernel"` NamedInterface로 병합 공개. outbound 엣지는 `sharedkernel`·`privacy`뿐(`HexagonalArchitectureTest.matching_must_not_depend_on_other_modules`가 강제 — PRIVACY 전략이 `FidaPlannedOrder` 등 privacy 계획 데이터를 읽어야 해 sharedkernel과 별개로 privacy만 허용). domain/model·domain/strategy는 Spring 비의존 — 빈 배선은 `trading`의 `CycleStrategyBeanConfig`(`@Configuration` 팩토리)가 전담하고, `stats`의 `BacktestEngine`은 순수 도메인 원칙으로 직접 `new` 한다. `adapter/in/web`은 예외 — 이 모듈 유일의 Spring 어댑터 계층으로, matching 타입을 root에 직접 노출하지 않기 위한 내부 전용 컨트롤러가 있다(아래 참고)
  domain/model/       ← `AccountBalance`(순수 잔고 record — `buyTotal(List<PlannedOrder>)`/`hasSufficientDepositFor`/`applyExecutions`; `Execution→Fill` 변환은 호출부인 TradingReporter/AdminTradeCorrectionService/BacktestEngine이 각자 담당)/`BootstrapPosition`/`InfinitePosition`/`PlannedOrder`/`ReverseModePosition`/`StrategyVrDetail`(`gradientAt`/`poolLimitRateAt` 램프 공식 — VR 공식 SSOT)/`VrPosition` — domain/strategy와 함께 `"kernel"` NamedInterface로 병합 공개. 현재가+전일종가 스냅샷은 `broker.domain.model.PriceSnapshot`을 직접 쓴다(커널은 `BigDecimal` 스칼라만 받아 자체 타입 불필요). `PlannedOrder`의 direction/orderType 필드는 `sharedkernel.OrderDirection`/`OrderType` 참조, `OrderTiming`은 `com.kista.sharedkernel` 소유(broker/privacy에 대응 개념 없이 여러 모듈이 공유해 sharedkernel로 이관)
  domain/strategy/    ← `CycleOrderStrategy` 계열 전략 구현 클래스(Infinite/ReverseInfinite/Privacy/VrStrategy + 대응 `*CycleOrderStrategy` 4종) 전체 — `CycleOrderStrategy` capability 패턴 SSOT(아래 "CycleOrderStrategy Capability 패턴" 참고), `PriceCapPolicy`(매수 가격 캡 배수 SSOT). `PlanContext`(전략 계산 입력)는 `Strategy` 전체가 아닌 `StrategyTicker ticker`만 보유 — 커널이 실제로 쓰는 값만 주입해 matching↔trading 순환을 방지한다
  adapter/in/web/     ← internal(비공개) — `StrategyCapabilityInternalController`(`GET /api/internal/matching/strategy-capabilities/{type}`) + `StrategyCapabilityResponse`(own-type — root `com.kista.web.dto.StrategyCapability`가 소비). root `MetaController`/`StrategyTypeMeta`가 과거 `CycleOrderStrategy`를 직접 import하던 것을 이 내부 API 호출로 대체(root→matching 컴파일 의존 제거 목적) — `cycleStrategies` 빈은 여전히 `trading`의 `CycleStrategyBeanConfig`가 배선하며, 이 컨트롤러가 Spring 계층에서 그 빈을 주입받아 노출만 한다

com.kista.finance/   ← Spring Modulith 이전 모듈(CLOSED) — 가계부 애그리게이트. 내부는 동일 Hexagonal 레이어 유지
  domain/model/      ← AssetSnapshot/FinanceAccount/FinanceBudget/FinanceCategory/FinanceGroup/FinanceTransaction/MonthlyClosing 등 record + Command — "domain" NamedInterface로 공개
  application/usecase/  ← UseCase 인터페이스(9개) — "usecase" NamedInterface로 공개
  application/port/output/ ← *Port 접미사 포트(7개) — "port" NamedInterface로 공개
  application/service/  ← FinanceAccountService/FinanceBudgetService/FinanceCategoryService/FinanceGroupService/FinanceTransactionService/AssetSnapshotService/BulkFinanceRegisterService/MonthlyClosingService/FinanceRegistrationReminderNotifier + MonthlyClosingGuard(package-private) — 모두 internal(외부 비공개)
    - **마감월 쓰기 차단**: `MonthlyClosing.completed=true`인 달은 자산 스냅샷·거래의 create/update/delete/shareToGroup/unshare를 `MonthClosedException`(409)으로 전면 차단한다. `MonthlyClosingGuard.verifyMonthOpen(currentGroupId, userId, date)`가 SSOT — `AssetSnapshotService`/`FinanceTransactionService` 두 서비스가 주입해 쓴다. update는 기존 날짜+대상 날짜 양방향 검사. 마감 스코프는 `MonthlyClosingPort.isMonthClosed`의 either/or(현재 그룹 있으면 그룹 마감 행, 없으면 개인 마감 행) — `findMyScope`의 union과 다르다(그룹 소속 유저는 개인 소유 record도 그룹 마감으로 판정). `MonthlyClosingService.upsert`(마감 해제)는 절대 가드 대상 아님. bulk 등록은 항목별 try/catch라 마감월 항목만 실패 수집
  adapter/in/web/     ← Finance*Controller/AssetSnapshotController/MonthlyClosingController/AdminFinanceCategoryController(경로만 /api/admin/**, finance 소유 유지) + dto/
  adapter/in/schedule/ ← FinanceRegistrationReminderScheduler
  adapter/out/persistence/ ← Entity + *JpaRepository + *PersistenceAdapter 3종

com.kista.notify/    ← Spring Modulith 이전 모듈(CLOSED) — Telegram/FCM 알림 발송 애그리게이트. 얇은 게이트웨이 모듈(자체 UseCase 없음, 레거시 application.usecase를 그대로 소비) — application.port.output만 공개 계약("port" NamedInterface), application/adapter는 internal. domain/model엔 SSE 전용 값 객체 `TradeEventView`(아래 참고) 하나만 있고 별도 NamedInterface 없음(모듈 내부에서만 쓰는 포트 시그니처 타입)
  application/port/output/ ← NotifyPort/UserNotificationPort/FcmDeviceTokenPort/RealtimeNotificationPort — "port" NamedInterface로 공개. RealtimeNotificationPort(SSE 실시간 알림 — `notifyStatusChange(UUID, UserStatus)`/`notifyTrade(UUID, TradeEventView)`)는 SSE 레지스트리와 함께 위치(아래 adapter/out/sse 참고). `TradeEventView`(`domain/model`)는 trading-core `com.kista.trading.notify.domain.model.TradeEventView`(발행측)와 필드 shape byte-identical own-type 복제 — Redis Pub/Sub(`RedisTradeEventSubscriber`)가 JSON 계약으로만 동기화되며 root는 trading-core 타입을 import할 수 없어(Gradle 컴파일 경계, 순환 불가피) 양쪽이 각자 own-type을 든다(constraints.md "모듈 경계 own-type" (a) 원장 참고). Task17의 `TradeLegSummary`(sharedkernel, 이벤트 payload용 `Execution` narrowing)와는 별개 타입
  adapter/in/telegram/ ← TelegramWebhookController + TelegramBotService, TelegramApiClient(package-private) + TelegramUpdate
  adapter/in/web/     ← FcmController/TradeStreamController(매매 이벤트 SSE)/StatusStreamController(`GET /api/auth/status-stream`) + dto/FcmTokenRequest
  adapter/out/gateway/ ← TelegramAdapter(관리자봇), CompositeUserNotificationAdapter → TelegramUserNotificationAdapter + FcmAdapter(사용자 알림), TelegramBotInfoAdapter/TelegramHttpClient/TelegramConfig/TelegramProperties/FcmConfig + 이벤트 리스너 11종(CycleEndedNotifier/CycleLifecycleNotifier/OrderCancelFailureNotifier/TradingReportNotifier/UserDeletedNotifier/TradingAlertNotifier/MarketAlertNotifier/PrivacyAlertNotifier/StatsAlertNotifier/SchedulerNotifier/UserFcmCleanupListener) — TradingAlertNotifier(TradingErrorEvent/MarketClosedEvent/MarketOpenEvent/MarketCloseEvent/BatchInterruptedEvent/InsufficientBalanceEvent 6개), OrderCancelFailureNotifier(OrderCancelFailedEvent), PrivacyAlertNotifier(PrivacyAlertRaisedEvent) 전부 `com.kista.sharedkernel` 소유 이벤트만 `@TransactionalEventListener(fallbackExecution=true)`로 구독한다(Task17 fix round 2 — root가 trading-core/privacy 컴파일 의존 없이 구독할 수 있도록 9개 전부 승격, 아래 sharedkernel 절 참고) — trading/privacy는 notify 포트를 직접 호출하지 않고 이벤트만 발행. SchedulerNotifier는 platform `SchedulerLifecycleEvent`를 구독
  adapter/out/sse/    ← SseEmitterRegistry(사용자별 SSE)/TradeSseEmitterRegistry(매매 이벤트 SSE) — `sse` 경로 세그먼트 유지 필수(`HexagonalArchitectureTest.sse_emitter_registry_must_not_be_used_in_application_layer`의 `com.kista..adapter.out.sse..` 와일드카드가 이 경로로 매치)
  adapter/out/persistence/ ← FcmDeviceTokenEntity + FcmDeviceTokenJpaRepository + FcmDeviceTokenPersistenceAdapter

com.kista.broker/    ← Spring Modulith 이전 모듈(CLOSED) — KIS/Toss/Mock 증권사 연동 애그리게이트. "domain"·"port"·"application" 3개 NamedInterface 공개, adapter/out은 의도적으로 비공개(모듈 내부 구현)
  domain/model/       ← Currency/DailyTransaction*/Execution/MarginItem/PresentBalanceResult 등 공통 불변 값 객체(direction/orderType 필드는 `sharedkernel.OrderDirection`/`OrderType` 참조) + broker 단독 소유 타입(쌍둥이 없음): PriceSnapshot(현재가+전일종가, `BrokerPricePort` 반환 + `prevCloseOrNull` 헬퍼 — trading이 직접 소비)/BrokerBalance(LiveBalancePort 반환값 — trading이 이 값으로 자신의 AccountBalance를 구성)/OrderInstruction·OrderResult(BrokerOrderCorrectionPort.place() 요청/응답)/CancelInstruction(BrokerOrderCorrectionPort.cancel() 요청)/PlacedOrderView·PositionView·StrategyRefLite(id+ticker, MockSimulationDataPort 반환용 얇은 뷰)/BrokerAccountRef(id/appKey/secretKey/accountNo/brokerAccountCode + broker(`sharedkernel.Broker`), account.Account 전체를 포트 시그니처에 노출하지 않기 위한 자격증명 투영 record — Account→BrokerAccountRef 변환은 `Account.toBrokerRef()` 인스턴스 메서드 1곳이 담당한다. broker 모듈 코드는 Account를 전혀 참조하지 않는다)/SellableQuantity/BrokerCredentialException·BrokerRateLimitException(broker-native — account 측엔 대응 타입 없음. KIS/Toss 인증 실패 시 KisAuthApi/TossAuthApi가 throw, GlobalExceptionHandler가 422/429 매핑)
  domain/model/kis/   ← KisApiException 등 KIS 전용 도메인 모델
  domain/model/toss/  ← Toss 전용 도메인 모델 — domain/model과 함께 "domain" NamedInterface로 병합 공개
  application/port/output/ ← 브로커 Capability 인터페이스(*Port) 총 16개 — 공통 7개(KIS/Toss/Mock 모두 구현) + BrokerAdapterPort(라우팅 마커) + BrokerConnectionTestPort(*AuthApi 클래스가 구현) + BrokerTokenCachePort(KisTokenPersistenceAdapter가 구현) + MockSimulationDataPort(MockBrokerAdapter 전용 — AlpacaCalendarAdapter→MarketHolidayStorePort 패턴의 역방향 적용: 데이터를 필요로 하는 broker가 포트를 정의하고, 데이터를 가진 trading이 `trading.adapter.out.MockSimulationDataAdapter`로 구현) + Toss 전용 5개. "port" NamedInterface로 공개. BrokerConnectionTestPort: 계좌 등록 전 검증이라 Account 없이 broker enum으로 라우팅 — verifyAccount→brokerAccountCode(KIS: null, Toss: accountSeq). 10개 포트(BrokerAdapterPort/BrokerAccountPort/BrokerOrderCorrectionPort/SellableQuantityPort/ExecutionPort/LiveBalancePort/PortfolioPort/BrokerPricePort/MarginPort/BrokerConnectionTestPort)는 `Account` 대신 `BrokerAccountRef`(위 domain/model own-type)를 시그니처에 사용 — StockInfoPort는 원래부터 Account 의존 없어 무변경
  application/service/ ← BrokerAdapterRegistry(public, require(BrokerAccountRef, Port.class)/find() — BrokerAccountRef.broker()로 라우팅)/BrokerConnectionTesters(계좌 등록 전 자격증명 검증, of(Broker) — sharedkernel.Broker)/BrokerCallGuard — "application" NamedInterface로 공개
  adapter/in/web/     ← internal(비공개) — CandleInternalController(`/api/internal/broker/candles/latest`, X-Internal-Token 인증) — root market 모듈이 `CandlePort`/`TossCandle`을 직접 참조하지 않도록 하는 내부 전용 엔드포인트, `market.adapter.out.internal.CandleQueryHttpAdapter`가 소비. `CandlePort.getLatestCandles(symbol, interval, count)`를 위임하되 `TossCandle`(broker 도메인 타입)을 그대로 반환하지 않고 own-type `CandleResponse`(컨트롤러 내부 record, 필드 shape byte-identical)로 매핑해 반환
  adapter/out/kis/    ← KisHttpClient(공통 헤더 + executeWithRetry: 401 시 거절된 토큰을 조건부 무효화한 후 최신 토큰으로 1회 재시도)/KisAuthApi/KisOrderApi/KisPriceApi/KisTradingApi/KisResponseParser/KisExchangeRegistry/KisConfig/KisTokenCoordinator/KisBrokerAdapter(BrokerConnectionTestPort는 KisAuthApi가 구현)
  adapter/out/toss/   ← TossHttpClient/TossConfig/TossAuthApi/TossCandleApi/TossHoldingsApi/TossOrderApi/TossPriceApi/TossMarketApi/TossResponseParser/TossResult/TossMarketCalendarCache/TossStockInfoCache/UsdKrwRateCache
                        TossDistributedTokenCoordinator + TossRedisTokenStore(계좌·관리자 Redis canonical token; TTL owner lease+원자적 generation INCR; generation counter/canonical generation을 비교하는 Lua fencing CAS; owner-safe unlock; SHA-256 최근 발급 fingerprint 2초 TTL) — 관리자(admin) 토큰은 Account가 없어 TokenCoordinator 범위 밖 별도 public 메서드(TossTokenStore)
                        TossBrokerAdapter(공통 7개 + Toss 전용 5개 Port 구현; BrokerConnectionTestPort는 TossAuthApi가 구현)
  adapter/out/mock/   ← MockBrokerAdapter(BrokerConnectionTestPort는 MockAuthApi가 구현) — 증권사 API 호출 없이 DB(cycle_position/orders) 기반 잔고·체결 시뮬레이션. 시세는 com.kista.broker.adapter.out.marketdata.CommonMarketPriceFeed 경유
                        getLiveBalance()의 usdDeposit은 계좌 내 전략 전체 합산값(TradingOrderBudgetAllocator가 대표 전략 1개로 계좌 전체 BUY 예산을 판단하는 기존 계약에 맞춤 — 전략별 값을 그대로 반환하면 다른 전략 잔고로 오판정)
  adapter/out/internal/ ← TokenCoordinator(계좌 토큰 obtain/recover 공통 계약 — 순수 adapter 내부 인터페이스, KIS/Toss 둘 다 구현하지만 폴리모픽 주입 지점은 없음: KisAuthApi/TossAuthApi 각자 구체 타입을 직접 주입)/DoubleCheckedTokenCache(KisTokenCoordinator 전용 JVM 내 토큰 캐시 — 1차 조회 → miss 시 계좌별 락 → 2차 double-check → 신규 발급; `BrokerTokenCachePort.saveToken`/`invalidateToken`은 REQUIRES_NEW로 락 해제 전 독립 커밋)/PrevCloseCache(전일종가 캐시, 현재 사용처는 TossPriceApi뿐)
  adapter/out/persistence/ ← KisTokenEntity + KisTokenJpaRepository + KisTokenPersistenceAdapter

com.kista.trading/   ← Spring Modulith 이전 모듈(CLOSED) — 주문/사이클 실행 이력/주문생성 전략 + 계좌별 영속 전략 설정(Strategy) 애그리게이트. 2026-09-12(Task 3)부터 `com.kista.stats`의 trading 소유 몫(계좌·Toss 통계, 백테스트, 포트폴리오 조회, summary/equity-curve/cycles 통계)도 `stats/` 서브패키지로 흡수(신규 모듈 아님, 기존 usecase/port/domain NamedInterface에 병합). "domain"·"usecase"·"port"·"schedule"·"stats" 5개 NamedInterface 공개 — application.service·adapter.out.*은 의도적으로 비공개(모듈 내부 구현). `application/event/` 패키지는 Task17 fix round 2에서 마지막 6개 이벤트(OrderCancelFailedEvent/TradingErrorEvent/MarketClosedEvent/MarketOpenEvent/MarketCloseEvent/BatchInterruptedEvent)까지 전부 `com.kista.sharedkernel`로 승격되며 완전히 비어 `package-info.java`째로 삭제됐다 — "event" NamedInterface 소멸(아래 sharedkernel 절 참고)
  domain/model/       ← 주문(`Order` — `matching.PlannedOrder`·`sharedkernel.{OrderDirection,OrderType,OrderTiming,OrderStatus}` 참조, `fromPlanned`/`toPlanned`로 커널과 상호 변환) + 사이클 실행 이력( 등, `TradingReport`는 `com.kista.sharedkernel` 소유로 이관됨 — 위 sharedkernel 절 참고) + 버전별 실행 파라미터(StrategyVersion/StrategyInfiniteDetail — `StrategyVrDetail`은 `com.kista.matching.domain.model` 소유, 아래 "com.kista.matching/" 참고) + VrSummary(VR 전략 조회 응답 요약) + 전략 설정 애그리게이트(Strategy/StrategyDetail/RegisterStrategyCommand/UpdateStrategyCommand/StrategySeedPreview/StrategySummary) 불변 값 객체 — position 값객체(InfinitePosition/ReverseModePosition/VrPosition/BootstrapPosition)·`AccountBalance`·`PlannedOrder`는 `com.kista.matching.domain.model` 소유 — domain.strategy와 함께 "domain" NamedInterface로 병합 공개
  domain/strategy/    ← 전략 *등록 정책* 리졸버만 잔존(StrategyCreationResolver(s)/InfiniteCreationResolver/PrivacyCreationResolver/VrCreationResolver/StrategyCreationRequest) — `CycleOrderStrategy` 계열 실행 커널(Infinite/ReverseInfinite/Privacy/VrStrategy, capability 패턴, PriceCapPolicy)은 커널 추출로 `com.kista.matching.domain.strategy`로 이전됐다(아래 "com.kista.matching/" 참고, "CycleOrderStrategy Capability 패턴" SSOT 위치도 이동). "domain" 이름으로 domain/model과 병합 공개. `StrategyCreationRequest`(원시값 5개, 리졸버 4종이 소비)는 19필드 `RegisterStrategyCommand` 전체를 넘기지 않기 위한 ISP 좁히기 — own-type이 아니라 정당한 narrowing이라 유지
  application/usecase/ ← TradingExecutionUseCase/VrReconfigureUseCase/VrStrategyDetailUseCase(VR 버전·사이클 상세 저장·요약 조립)/StrategyUseCase(조회/등록/수정/삭제/일시정지/재개 + 전략 스코프 read: 시드 미리보기·사이클 이력·주문 내역) — TradingCycleController가 참조. "usecase" 이름으로 공개. **`StrategyUseCase`는 command/query 미분리 유지** — 유일 소비자(`TradingCycleController`)가 양쪽 전체 surface를 다 쓰므로 ISP 분리 이득이 없고, `StrategyService`가 `toDetail`/`assemble`을 command·query가 공유해 서비스 분리 시 헬퍼 중복만 생긴다. 이 판정은 `toDetail`/`assemble`을 실제로 공유하는 delete/pause/resume/list*/getById/update 범위에만 적용된다 — register()와 조회 3종(시드 미리보기·사이클 이력·주문 내역, `toDetail`/`assemble` 미사용)은 `StrategyCreationService`/`StrategyHistoryQueryService`로 내부 분리돼 있다(`StrategyUseCase` 유일 구현체인 `StrategyService`는 그대로 유지 — 인터페이스 미분리 결정과 무충돌). 분리 트리거: `toDetail`/`assemble` 공유 범위에 query만 필요한 2번째 소비자(admin read 경로 등) 등장 시
  application/port/output/ ← OrderPort/CyclePositionPort/CyclePositionInfiniteDetailPort/StrategyCyclePort/StrategyCycleVrPort/TradingErrorReportPort/StrategyVersionPort/StrategyInfiniteDetailPort/StrategyVrDetailPort/StrategyPort(10개) + HeartbeatPort(스케쥴러 dead-man's switch, trading 스케쥴러 전용이지만 기존 관례대로 "port"로 공개, 외부 소비자 0) + TradingUserProfilePort(trading 정의·trading 자체 구현 — 구현체는 같은 모듈의 `adapter/out/persistence/UserNotifyProfilePersistenceAdapter`이며 `kista.user_notify_profile` 복제 테이블만 읽는다. 과거 `com.kista.web.trading.TradingUserProfileAdapter`(user 직접 참조)를 쓰던 포트 역전은 root의 trading-core 컴파일 의존 0 목표와 애초에 양립 불가라 폐기됐다 — 아래 user_notify_profile 항목 및 constraints.md "폐기된 전례" 참고) — "port" 이름으로 공개(`StrategyCreationPolicyPort`는 `com.kista.sharedkernel.port` 소유로 승격돼 이 목록에서 빠짐 — 위 sharedkernel 절 참고, trading은 여전히 소비만 한다)
  application/service/ ← internal(비공개) — TradingExecutionFacade(preview/executeManually/cancelOrder/cancelByCycle/execute/executeBatch 단일 진입점), TradingService(배치·단건 실행 최상위 오케스트레이션 — 대기·리포트 순서만 남음)
                         TradingBatchGuard(전략별 단계 실행 격리 가드 — runSafely/notifyErrorSafely/notifyBatchInterrupted, TradingService·TradingCandidatePlanner 공유) + TradingCandidatePlanner(후보수집+계좌별 예산 배정 — executeBatch/placeOpenOrders 공용)
                         PreviewDepositCache — TradingBuyCompetitionSimulator 전용 계좌 단위 라이브 예수금(usdDeposit) 짧은 TTL(3초) 캐시 + 계좌별 락으로 동시 miss 단일화. usdDeposit은 ticker 무관 계좌 전체 값이라 계좌당 전략 N개의 preview 병렬 호출을 실제 조회 1회로 축소. 실주문 집행 경로(ManualTradingService/TradingOrderBudgetAllocator)는 미사용 — 항상 최신값 직접 조회
                         TradingOrderBudgetAllocator — 계좌별 slot-aware BUY/SELL 독립 예산 배정 (우선순위·실패 격리·제외 규칙 상세 → workflow.md "스케쥴러 주문 예산 배정")
                         live 잔고·판매가능수량 조회는 com.kista.broker.application.service.BrokerAdapterRegistry.require(account, LiveBalancePort/SellableQuantityPort.class) 직접 라우팅 — 별도 Router 클래스 없음 (KIS: TTTS3012R fetchHolding 재사용 / Toss: /api/v1/sellable-quantity)
                         CyclePositionPersistor: 포지션 스냅샷 저장 + 사이클 종료·rotation + VrCycleRolloverService.rollIfDue() 호출 (VR 예외 → "VR 전략 패턴")
                         StrategyService(StrategyUseCase 구현)/AccountCascadeListener(AccountDeletedEvent 구독)/StrategyUserCascadeListener(UserDeletedEvent 구독) — 전부 internal (`ActiveStrategyCountAdapter`는 `com.kista.web.trading`으로 이전, 아래 "com.kista.web/" 참고)
  adapter/in/schedule/ ← TradingOpenScheduler/TradingCloseScheduler/BatchContextFactory(전략 목록 → BatchContext 빌드, 조회 실패 시 skip + notifyError) — Task 10부터 root가 이 스케쥴러 빈을 직접 주입하지 않는다(:trading-core→:api 컴파일 경계라 애초에 역방향 주입 불가). 수동 트리거는 `TradingSchedulerInternalController`(아래 adapter/in/web 참고)가 내부 API로 대신 실행한다. "schedule" 이름으로 공개(외부 직접 소비자는 없으나 기존 NamedInterface 관례 유지)
  adapter/in/web/      ← internal(비공개, TradingSchedulerInternalController 1개만 예외) — OrderCancelController + TradingCycleController(사이클 CRUD·pause/resume·수동 실행·VR 재설정·전략 이력/주문/시드 미리보기 — trading "usecase"만 소비하는 단일 모듈 컨트롤러) + dto/(TradingCycleRequest/TradingCycleResponse·VrConfigRequest·StrategySeedPreviewResponse·StrategyOrdersResponse·CancelOrdersResponse·ExecuteOrdersResponse·NextOrdersResponse·CycleHistoryPageResponse/CycleHistoryResponse — CycleHistoryPageResponse/CycleHistoryResponse는 stats `adapter/in/web/dto` 사본과 byte-identical own-type)
  adapter/in/web/TradingSchedulerInternalController ← Task 10 신설 — `POST /api/internal/trading/scheduler/{open,close}`(X-Internal-Token, 202 ACCEPTED, 바디 없음). `TradingOpenScheduler`/`TradingCloseScheduler`를 직접 주입해 가상 스레드로 백그라운드 실행 — root의 `AdminTradingSchedulerController`(admin, 아래 "com.kista.admin/" 참고)가 `TradingSchedulerCommandPort`/HTTP 어댑터 경유로 호출하는 원격 트리거 진입점
                        TradingExceptionHandler(`@RestControllerAdvice(basePackages={trading/account/privacy/trading.stats/broker/marketcalendar/matching 7개 adapter.in.web}, @Order(HIGHEST_PRECEDENCE))` — trading-core 네이티브 컨트롤러 전용 예외 6종(`BrokerCredentialException`/`BrokerRateLimitException`/`ManualTradingException`/`OrderCancelException`/`PrivacyTradeConflictException`/`Account.DuplicateAccountException`) 매핑 + `GENERIC_MAPPINGS`/`handleGeneric`(`SecurityException`→403/`NoSuchElementException`→404/`IllegalArgumentException`→400 등 범용 프레임워크 예외, `isClientDisconnect` 가드 포함 — root `GlobalExceptionHandler.handleAll`과 동일 로직 복제). trading-core가 별도 프로세스(TradingApplication)로 분리되며 root `GlobalExceptionHandler`와 같은 JVM에 공존하지 않아 fallthrough가 성립하지 않으므로, 범용 예외 매핑도 이 클래스가 자체 보유 — 위 "com.kista.web/" GlobalExceptionHandler 항목 참고
  adapter/out/         ← internal(비공개) — MockSimulationDataAdapter(broker.application.port.output.MockSimulationDataPort 구현 — broker의 MockBrokerAdapter가 필요로 하는 cycle_position/orders 데이터를 trading이 제공)
  adapter/out/heartbeat/ ← internal(비공개) — HeartbeatAdapter/HeartbeatConfig/HeartbeatProperties(healthchecks.io 핑)
  adapter/out/persistence/ ← (user_notify_profile 복제본, internal) UserNotifyProfileEntity + UserNotifyProfileJpaRepository + UserNotifyProfilePersistenceAdapter(`TradingUserProfilePort` 구현) + UserNotifyProfileSyncListener — trading-core가 소유하는 사용자 알림·잔고검증·활성여부 읽기 전용 복제본(`kista` 스키마). 원본은 user 소유 `public.users`/`user_settings`/`user_notification_prefs`. root·trading-core는 별도 프로세스라 같은 JVM Modulith EPR로는 이벤트가 전달되지 않는다(4a 분리 이후 확인된 드리프트, → constraints.md "이벤트 클래스 패키지 이동은 event_publication에 ClassNotFoundException을 남긴다" 인접 서술과 별개 사고) — root `com.kista.user.adapter.out.redis.UserEventStreamPublisher`가 `UserNotifyProfileChangedEvent`(upsert)·`UserDeletedEvent`(삭제)를 각각 `stream:user.notify-profile.changed`/`stream:user.deleted`(Redis Stream, 키·컨슈머그룹명은 `shared`의 `com.kista.platform.redis.RedisStreamConfig`)로 XADD 발행하고, trading-core `com.kista.trading.adapter.in.redis.UserEventStreamConsumerConfig`가 컨슈머 그룹(`trading-core`)으로 구독해 `UserEventStreamBridge`가 자기 프로세스 안에서 `ApplicationEventPublisher.publishEvent()`로 로컬 재발행한 뒤에야 이 리스너가 `@TransactionalEventListener`로 받는다 — root 쪽 로컬 리스너(finance cascade·UserDeletedNotifier·UserFcmCleanupListener)는 기존 그대로 병행 유지, 대체 아님. `fallbackExecution=true`는 발행 측(`UserEventStreamPublisher`)에 필요(`UserService.login()`의 ADMIN 승격이 `@Transactional(NOT_SUPPORTED)` 구간이라 앰비언트 트랜잭션이 없어, 없으면 그 발행이 통째로 버려진다). 컨슈머 크래시 대비 pending 항목 복구는 `UserEventStreamRecoveryScheduler`(5분 주기 XAUTOCLAIM)가 담당. `is_active`는 `UserStatus.ACTIVE` 여부 전용 컬럼으로 `findAllActive()`(장 개장·마감 브로드캐스트 대상)가 이 컬럼으로만 판별한다 — `TradingUserProfile` record에는 넣지 않는다(포트가 거르고 투영은 모른다). 기존 사용자 백필은 `V22__create_user_notify_profile.sql`이 담당한다(복제본이 비면 `findAllByUserIds()`가 빈 맵을 돌려 BatchContextFactory가 전 계좌를 건너뛴다 = 매매 전면 중단)
  adapter/out/persistence/ ← internal(비공개) — Order/CyclePosition/CyclePositionInfinite/StrategyCycle/StrategyCycleVr Entity + *JpaRepository + *PersistenceAdapter 3종 구성, PersistenceSupport. StrategyVersion/StrategyInfiniteDetail(StrategyInfinite*)/StrategyVrDetail(StrategyVrVersion*) Entity + *JpaRepository + *PersistenceAdapter 9파일도 이 아래. StrategyEntity + StrategyJpaRepository + StrategyPersistenceAdapter 3파일도 이 아래
  stats/domain/model/  ← InvestmentPoint(투자 누적지수 시점 record)/BenchmarkGranularity(MONTHLY/DAILY/WEEKLY enum) — trading 소유 CyclePosition/StrategyCycle 기반 통계 값 객체. root `com.kista.stats`는 2026-09-11 서비스 분리 설계(`2026-09-11-kista-trading-service-split-design.md` 3단계)의 `:api → :trading-core` 컴파일 의존 0 게이트를 막고 있었기에(Task5의 HTTP 내부 API 전환에도 `InvestmentPointsPort.Result` 시그니처가 이 두 타입을 직접 노출), Task 11(2026-09-14)에서 own-type 전환이 완료됐다 — `com.kista.stats.domain.model`에 동일 shape의 `InvestmentPoint`/`BenchmarkGranularity` 복제본을 신설하고 `InvestmentPointsPort`/`InvestmentPointsHttpAdapter`/`StatsService`/`HousingBenchmarkComparisonBuilder`의 import를 교체했다(포트 시그니처 own-type 복제 게이트 (a) 순환 불가피 적용, HTTP 호출 로직 자체는 무변경). StatsSummary/EquityCurve/EquityPoint/CyclePerformancePage/CyclePerformance/StrategyTypeStats(summary/equity-curve/cycles 도메인 타입)도 이 패키지 소속 — 2026-09-12 Task 3에서 stats에서 이관된 **영구 소유**분(`ReturnMetrics`는 이후 `com.kista.sharedkernel`로 재승격돼 이 패키지 소속에서 빠짐 — 위 sharedkernel 절 참고). stats/application과 함께 "stats" NamedInterface로 병합 공개
  stats/application/   ← MonthlyReturnCalculator(public — CyclePosition/StrategyCycle로 시간가중수익률·투자지수를 계산하는 순수 계산기, 로직 SSOT는 코드) — "stats" 이름으로 domain/model과 병합 공개
  stats/application/usecase/ ← AccountStatisticsUseCase/PortfolioUseCase(notify TelegramBotService가 `/portfolio` 명령 응답에 소비)/TossStatisticsUseCase/BacktestUseCase/TradingStatsUseCase(summary/equity-curve/cycles — 구 stats `UserStatsUseCase`의 해당 로직 이관분, Task 4에서 전용 컨트롤러 `TradingStatsController`로 분리 완료, 아래 adapter/in/web 참고) — trading의 application.usecase와 함께 "usecase" 이름으로 병합 공개
  stats/application/service/ ← internal(비공개) — AccountStatisticsService/PortfolioService/BacktestService/TossStatisticsService/BrokerStatisticsRouter/TradingStatsService(TradingStatsUseCase 구현) + TradingStatsResultCache(summary/equity-curve 전용 5분 TTL 캐시 — root `com.kista.stats.application.service.StatsResultCache`와는 별개 인스턴스, 벤치마크 비교 10분 TTL 캐시와 분리) — 전부 2026-09-12 Task 3에서 stats에서 이관
  stats/application/port/output/ ← 소유 포트 없음 — `HistoricalCandlePort`(과거 일봉 조달, root `AlpacaIndexPriceAdapter`가 `IndexPriceFeedPort`와 함께 구현)는 `com.kista.sharedkernel.port`로 재승격돼 이 패키지 소속에서 빠짐 — 위 sharedkernel 절 참고
  stats/domain/backtest/ ← internal(비공개) — BacktestEngine/FillSimulator(백테스트 시뮬레이션 엔진). BacktestEngine은 `com.kista.matching`(주문생성 커널)을 직접 `new`해 소비한다
  stats/domain/model/backtest/ ← BacktestCommand/BacktestPoint/BacktestResult/BacktestSummary(백테스트 커맨드·결과 4타입) — trading의 domain.model+domain.strategy와 함께 "domain" 이름으로 병합 공개. `DailyCandle`은 `com.kista.sharedkernel`로, 이를 소비하던 `HistoricalCandlePort`는 `com.kista.sharedkernel.port`로 재승격돼 이 패키지 소속에서 빠짐 — 위 sharedkernel 절 참고
  stats/adapter/in/web/ ← internal(비공개) — DashboardController/StatisticsController(KIS 전용 live)/TossStatisticsController(Toss 전용 live)/BacktestController/TradingStatsController(summary/equity-curve/cycles 3라우트 — Task 4에서 root `StatsController`의 임시 위임 배선을 대체해 신설)/TradingStatsInternalController(`/api/internal/trading/stats/investment-points` — Task 5에서 신설, root `com.kista.stats.adapter.out.internal.InvestmentPointsHttpAdapter`가 소비) + dto/ 16종( — CycleHistoryPageResponse/CycleHistoryResponse는 trading `adapter/in/web/dto` 기존 사본과, TossCandleResponse는 market 사본과 byte-identical own-type)

com.kista.market/    ← Spring Modulith 이전 모듈(CLOSED) — 공포탐욕지수(CNN/Crypto Fear & Greed Index) 애그리게이트. 미국 시장 휴장일 캘린더는 `com.kista.marketcalendar`로 분리됐다(아래 참고) — `market`은 root(`:api`) 전용이며 fear-greed만 남는다. "domain"(domain.model)·"port"(application.port.output)·"event"(application.event) 3개 NamedInterface 공개 — application.{usecase,service}·adapter는 의도적으로 비공개(모듈 내부 구현)
  domain/model/       ← FearGreedRating/FearGreedSnapshot + MarketSession(DIRECT/BLOCKED, `marketcalendar.domain.model.MarketSessionSnapshot.MarketSession` own-type 복제 — market↔marketcalendar 순환 방지)/TossDailyCandle(`broker.domain.model.toss.TossCandle` own-type 복제, 일봉 1건 — market↔broker 순환 방지, 필드 shape byte-identical) — "domain" NamedInterface로 공개
  application/port/output/ ← CnnFearGreedPort/CryptoFearGreedPort/FearGreedSnapshotPort 3개 + MarketCalendarQueryPort(marketcalendar 내부API 호출, `SessionView(MarketSession, isDst)` 반환)/CandleQueryPort(broker 내부API 호출, 일봉만 지원) 2개 — "port" NamedInterface로 공개
  application/usecase/ ← FetchFearGreedUseCase/GetFearGreedUseCase/MarketUseCase(캘린더/캔들 조회 — `getDailyCandles`는 `List<TossDailyCandle>` 반환) — 전부 internal(외부 소비자 없음, 모듈 내부 adapter/in/web·service에서만 참조, package-info/NamedInterface 없음). `MarketUseCase`는 `MarketHolidayService`가 구현 — `MarketCalendarQueryPort`(휴장일·세션, marketcalendar 내부API)와 `CandleQueryPort`(일봉, broker 내부API) 양쪽에 HTTP로 위임하며 marketcalendar/broker 타입을 직접 참조하지 않는다. `MarketHolidayController`가 소비
  application/event/  ← FearGreedFetchFailedEvent — notify 모듈이 `@TransactionalEventListener`로 구독(MarketAlertNotifier, CLOSED↔CLOSED 모듈 간 이벤트 교차). "event" 이름으로 공개
  application/service/ ← internal(비공개) — FearGreedQueryService/FearGreedService/MarketHolidayService(`MarketCalendarQueryPort`+`CandleQueryPort` 위임)
  adapter/in/web/     ← internal(비공개) — FearGreedController/MarketHolidayController + dto/(FearGreedResponse/MarketSessionResponse(`market.domain.model.MarketSession` own-type 소비)/TossCandleResponse(stats 사본과 byte-identical own-type))
  adapter/in/schedule/ ← internal(비공개) — FearGreedScheduler
  adapter/out/feargreed/ ← internal(비공개) — CnnFearGreedAdapter/CryptoFearGreedAdapter/FearGreedConfig
  adapter/out/internal/ ← internal(비공개) — MarketCalendarQueryHttpAdapter(`MarketCalendarQueryPort` 구현, `/api/internal/marketcalendar/**` 호출)/CandleQueryHttpAdapter(`CandleQueryPort` 구현, `/api/internal/broker/candles/latest` 호출, interval="1d" 고정)
  adapter/out/persistence/feargreed/ ← internal(비공개) — FearGreedSnapshotEntity + FearGreedSnapshotJpaRepository + FearGreedSnapshotPersistenceAdapter

com.kista.marketcalendar/ ← Spring Modulith 이전 모듈(CLOSED) — 미국 시장 휴장일 캘린더 애그리게이트. `market`(fear-greed)에서 분리됐고, `:trading-core` Gradle 서브프로젝트에 위치(trading의 유일한 market 계열 의존이 이 모듈로 좁혀진다). "domain"(domain.model)·"port"(application.port.output) 2개 NamedInterface 공개 — application.service·adapter는 의도적으로 비공개(모듈 내부 구현)
  domain/model/       ← MarketSessionSnapshot — "domain" NamedInterface로 공개
  application/port/output/ ← MarketCalendarPort/MarketCalendarRefreshPort/MarketHolidayStorePort 3개 — "port" NamedInterface로 공개
  adapter/in/web/     ← internal(비공개) — MarketCalendarInternalController(`/api/internal/marketcalendar/{holidays,is-open,session}`, X-Internal-Token 인증) — root market 모듈이 `MarketCalendarPort`/`MarketSessionSnapshot`을 직접 참조하지 않도록 하는 내부 전용 엔드포인트, `market.adapter.out.internal.MarketCalendarQueryHttpAdapter`가 소비. `session` 라우트는 own-type `SessionResponse(String session, boolean isDst)`로 응답
  adapter/in/schedule/ ← internal(비공개) — MarketCalendarRefreshScheduler
    - **2-role 이후 캘린더 부트스트랩 staleness**: `MarketCalendarRefreshScheduler`의 초기 적재(`ApplicationReadyEvent`, "기동 시 캘린더 초기 적재")는 `@ConditionalOnProperty(scheduler.enabled)`로 게이팅되므로 `kista-scheduler` 재기동 시에만 실행된다 — `kista-api` 재기동마다(잦음)가 아니다. 캘린더 데이터 self-heal 창이 수시간 → 수주로 늘어난다. 월간(매월 1일)·연간(1월 1일) 갱신 크론이 있어 무해하나, 캘린더 이상 시 `kista-scheduler`를 재기동하면 즉시 재적재된다는 점을 기억
  adapter/out/alpaca/  ← internal(비공개) — AlpacaCalendarAdapter/AlpacaConfig/AlpacaProperties (`com.kista.stats.adapter.out.alpaca`의 동명 클래스(AlpacaIndexPriceAdapter/AlpacaConfig/AlpacaProperties, 빈 이름 `alpacaRestClient`)와 빈 이름 충돌 방지를 위해 marketcalendar판만 `marketAlpacaConfig`/`marketAlpacaRestClient`로 명시적 개명)
  adapter/out/persistence/ ← internal(비공개) — UsMarketHolidayEntity + UsMarketHolidayJpaRepository + MarketCalendarPersistenceAdapter

com.kista.privacy/   ← Spring Modulith 이전 모듈(CLOSED) — FIDA 기준 매매표(PRIVACY 전략의 전역 SSOT 매매 계획) 애그리게이트. "domain"(domain.model)·"port"(application.port.output)·"usecase"(application.usecase) 3개 NamedInterface 공개 — application.service·adapter는 의도적으로 비공개(모듈 내부 구현). PRIVACY *전략 실행* 로직(PrivacyStrategy/PrivacyCycleOrderStrategy 등)은 trading 소유로 유지 — 이 모듈은 계획 데이터만 다룬다. `application/event/`(`PrivacyAlertRaisedEvent`)는 Task17 fix round 2에서 `com.kista.sharedkernel`로 승격되며 패키지째 삭제됐다 — "event" NamedInterface 소멸(아래 sharedkernel 절 참고)
  domain/model/       ← FidaOrderCommand/FidaPlannedOrder/PrivacyCurrentBase/PrivacyDates/PrivacyTradeBase/PrivacyTradeBaseView/PrivacyTradeConflictException/PrivacyTradeSaveResult/PrivacyTradeValidationReport 9개 record/enum/예외 — "domain" NamedInterface로 공개. FidaPlannedOrder/PrivacyTradeBase.PrivacyTrade의 direction/orderType 필드는 `sharedkernel.OrderDirection`/`OrderType` 참조. PrivacyDates.releaseDateFor()/tradeDateOf()는 FIDA 발행일↔거래일 업무 규칙 헬퍼(시간대 변환 아님) — BacktestService가 "domain"으로 소비
  application/port/output/ ← PrivacyTradePort — "port" NamedInterface로 공개
  application/usecase/ ← PrivacyUseCase(FidaOrderController가 소비)/PrivacyTradeValidationUseCase(trading의 TradingOpenScheduler가 소비) — "usecase" NamedInterface로 공개
  application/service/ ← internal(비공개) — PrivacyService(PrivacyUseCase 구현, notify 직접 호출 대신 sharedkernel `PrivacyAlertRaisedEvent` 발행)/PrivacyTradeValidationService
  adapter/in/web/     ← internal(비공개) — FidaOrderController(`/api/internal/**`, POST /fida-orders)/PrivacyInternalQueryController(admin의 PrivacyQueryHttpAdapter가 소비하는 목록 조회 `GET /api/internal/privacy/trade-bases`)/PrivacyBaseInternalController(admin의 createBase 재조회용 `GET .../trade-bases/{baseId}` + 관리자 수동 보정 `PATCH .../trade-bases/{baseId}`, `PATCH .../trade-bases/{baseId}/orders/{orderId}` — PrivacyTradePort.updateBase/updateOrder 그대로 위임) + dto/FidaOrderResponse
  adapter/out/persistence/ ← internal(비공개) — PrivacyTradeBaseEntity + PrivacyTradeBaseOrderEntity + PrivacyTradeBaseJpaRepository + PrivacyTradePersistenceAdapter

com.kista.stats/    ← Spring Modulith 이전 모듈(CLOSED) — 주택/ETF 벤치마크 비교·KB Land/Alpaca 지수 시세 수집 애그리게이트. 2026-09-12(Task 3)에 계좌·Toss 통계(AccountStatisticsService/TossStatisticsService/BrokerStatisticsRouter)·백테스트(BacktestService+Engine+FillSimulator)·포트폴리오 조회(PortfolioService)·summary/equity-curve/cycles 통계(구 `UserStatsUseCase`의 해당 로직)가 전부 `com.kista.trading.stats`로 이관됐다(위 "com.kista.trading/" 참고) — 이 모듈엔 housing/ETF 벤치마크 비교와 그 시세 수집만 남는다. "domain"(domain.model)·"usecase"(application.usecase)·"port"(application.port.output)·"event"(application.event)·"schedule"(adapter.in.schedule) 5개 NamedInterface 공개 — application.service·adapter.in.web(+dto/openapi)·adapter.out.*은 의도적으로 비공개(모듈 내부 구현). root `StatsController`의 summary/equity-curve/cycles 3개 라우트는 Task 4에서 trading-core 전용 컨트롤러 `TradingStatsController`로 완전히 분리됐다 — root `StatsController`는 더 이상 `TradingStatsUseCase`를 주입하지 않고 housing/ETF 벤치마크 비교 6개 라우트만 소유한다
  domain/model/       ← 벤치마크 비교 11타입(BenchmarkAssetType/BenchmarkScope/CurrentExchangeRate/EtfBenchmarkSymbol/HousingBenchmarkComparison/HousingBenchmarkPoint/HousingBenchmarkPrice/HousingBenchmarkRegion/HousingPriceIndex/IndexPrice/PerformanceComparisonSummary) — "domain" NamedInterface로 공개 (`InvestmentPoint`/`BenchmarkGranularity`/`ReturnMetrics`/`StatsSummary`/`EquityCurve`/`EquityPoint`/`CyclePerformancePage`/`CyclePerformance`/`StrategyTypeStats`는 `com.kista.trading.stats.domain.model` 소유로 이관, 위 "com.kista.trading/" 참고)
  application/usecase/ ← UserStatsUseCase(housing/ETF 벤치마크 비교 6개 메서드만 — summary/equity-curve/cycles 3개는 `TradingStatsUseCase`로 이관)/FetchHousingBenchmarkUseCase/FetchHousingPriceIndexUseCase/SyncMarketIndexPricesUseCase 4개 — "usecase" NamedInterface로 공개 (`BacktestUseCase`/`PortfolioUseCase`는 `com.kista.trading.stats.application.usecase` 소유로 이관)
  application/port/output/ ← HousingBenchmarkFeedPort/HousingBenchmarkPricePort/HousingPriceIndexPort/IndexPriceFeedPort/IndexPricePort 5개 — "port" NamedInterface로 공개 (`HistoricalCandlePort`는 `com.kista.sharedkernel.port` 소유로 승격 — 구현체 `AlpacaIndexPriceAdapter`는 그대로 이 모듈의 adapter/out/alpaca에 남아 승격된 포트를 구현)
  application/event/  ← StatsAlertRaisedEvent(String message) — notify 모듈이 `@TransactionalEventListener(fallbackExecution=true)`로 구독(StatsAlertNotifier, CLOSED↔CLOSED 모듈 간 이벤트 교차). "event" 이름으로 공개
  application/service/ ← internal(비공개) — StatsService(UserStatsUseCase 구현 — housing/ETF 벤치마크 비교 6개 메서드만; summary/equity-curve/cycles 로직은 `com.kista.trading.stats.application.service.TradingStatsService`로 이관)
                         StatsResultCache — 벤치마크 비교(housing-benchmark, ETF 포함) 10분 인메모리 TTL 캐시 전용으로 축소(summary/equity-curve 5분 캐시는 `TradingStatsResultCache`로 분리, Task 3에서 선행 신설). 단일 인스턴스 전제 — 다중 인스턴스 시 인스턴스별 캐시가 최대 TTL만큼 상이할 수 있음
                         HousingBenchmarkComparisonBuilder — Spring·포트 비의존 순수 계산 클래스(정규화 비교 조립). ETF 비교에도 재사용됨(이름은 HousingBenchmark). 시간가중수익률(TWR) 계산 자체는 `com.kista.trading.stats.application.MonthlyReturnCalculator`(trading 소유)가 담당하고 결과(InvestmentPoint)만 전달받아 조립
                         getHousingBenchmarkComparison: currentExchangeRate는 요청마다 실시간 조회하는 정보성 필드일 뿐 수익률·공통월·summary 계산에는 미반영(조회 실패 시 null 처리, 200 정상 반환) — 투자(USD)·벤치마크(HOUSING=KRW/ETF=USD) 현지통화 그대로 비교, 환율 변환 없음
                         HousingBenchmarkService/HousingPriceIndexService/MarketIndexPriceSyncService — KB Land/Alpaca 수집 실패 시 notify 직접 호출 대신 StatsAlertRaisedEvent 발행
  adapter/in/web/     ← internal(비공개) — StatsController(DB 근사 집계 — housing/ETF 벤치마크 비교 6개 라우트만 직접 구현, summary/equity-curve/cycles 3개 라우트는 Task 4에서 `TradingStatsController`로 분리 완료돼 더 이상 소유하지 않음) + openapi/HousingBenchmarkOpenApiCustomizer + dto/ 5종(HousingBenchmarkComparisonResponse/HousingBenchmarkRegionsResponse/HousingBenchmarkSeriesResponse/HousingPriceIndexSeriesResponse/EtfPriceSeriesResponse) — DashboardController/StatisticsController/TossStatisticsController/BacktestController/TradingStatsController/TradingStatsInternalController와 그 DTO(PortfolioSummaryResponse 등 13종, StatsSummaryResponse/EquityCurveResponse/CyclePerformancePageResponse 포함)는 전부 `com.kista.trading.stats.adapter.in.web`로 이관, 위 "com.kista.trading/" 참고
  adapter/in/schedule/ ← KbLandHousingBenchmarkScheduler/KbLandPriceIndexScheduler/MarketIndexPriceSyncScheduler — `com.kista.web.AdminSchedulerController`가 KbLand 스케쥴러 2개를 수동 트리거용으로 직접 주입(trading "schedule" NamedInterface와 동일 관례)해 공개. "schedule" 이름으로 공개. MarketIndexPriceSyncScheduler는 비거래일에도 Alpaca 빈 배열 반환으로 무해한 no-op이라 요일 조건 없음
  adapter/out/alpaca/  ← internal(비공개) — AlpacaIndexPriceAdapter/AlpacaConfig/AlpacaProperties (MarketIndexPrice* 계열 전용 — 빈 이름 `alpacaRestClient` 그대로, market판 `marketAlpacaConfig`와 무충돌)
  adapter/out/kbland/  ← internal(비공개) — KbLandHousingBenchmarkAdapter/KbLandConfig/KbLandProperties (KB Land 아파트 5분위 매매평균가격(월간) + 주간 매매가격지수 조회)
  adapter/out/persistence/housingbenchmark/ ← internal(비공개) — HousingBenchmarkPriceEntity/HousingPriceIndexEntity + *JpaRepository + *PersistenceAdapter 6개
  adapter/out/persistence/marketindex/ ← internal(비공개) — MarketIndexPriceEntity + MarketIndexPriceJpaRepository + MarketIndexPricePersistenceAdapter 3개

com.kista.admin/    ← Spring Modulith 이전 모듈(CLOSED) — 관리자 조회·정정·재정렬·감사로그·앱오류로그 + 런타임 설정 애그리게이트. "domain"(domain.model)·"usecase"(application.usecase)·"port"(application.port.output) 3개 NamedInterface 공개 — application.service·adapter.*는 의도적으로 비공개(모듈 내부 구현). event NamedInterface 없음(admin→notify 참조가 0건이라 이벤트 전환 대상 자체가 없음), schedule NamedInterface 없음(스케쥴러 수동 트리거는 KbLand는 여전히 `com.kista.web`, trading은 Task 10부터 admin 소유 `AdminTradingSchedulerController`로 분리 — 아래 adapter/in/web·"com.kista.web/" 참고). `adapter/out/aop/ErrorLogAspect`(`NotifyPort.notifyError` 인터셉트해 `AppErrorLogPort`로 저장, notify는 포인트컷 문자열이라 컴파일 의존 없음)도 internal
  domain/model/       ← flat 18타입 — 관리자 read-model/own-type 15() + 런타임 설정 3(RuntimeSettings/BenchmarkSettings/BenchmarkFieldSettings — `StrategyCreationSettings`/`StrategyFieldSettings`/`RecurringMode`는 `com.kista.sharedkernel` 소유) — "domain" NamedInterface로 공개. `AdminAccountView`(account.domain.model.Account의 own-type read model, Task 8)는 id/userId/accountNo(마스킹 전 원본)/broker/createdAt 5필드만 narrowing — nickname/appKey/secretKey/brokerAccountCode는 admin이 읽지 않아 제외(다른 own-type 대부분이 원본 전체 필드를 복제하는 것과 달리 보안상 의도적으로 좁힌 첫 사례, → constraints.md "모듈 경계 own-type" 원장). admin/settings 서브패키지 없음(AdminUserView는 com.kista.user.domain.model 소유. `AdminCycleStrategySummary`류 admin 전용 read-model은 없음 — admin이 trading `StrategySummary`를 직접 소비)
  application/usecase/ ← AdminQueryUseCase/AdminReorderUseCase/AdminStrategyUseCase/AdminTradeCorrectionUseCase/AdminUserUseCase/AdminSettingsUseCase/RuntimeSettingsUseCase 7개 — "usecase" NamedInterface로 공개(GetUserSettingsQuery는 com.kista.user 소유)
  application/port/output/ ← AccountQueryPort(Task 8 신설 — admin이 정의, AccountQueryHttpAdapter가 내부 API로 구현)/AppErrorLogPort/AuditLogPort/PrivacyQueryPort/RuntimeSettingsPort/TradingCommandPort/TradingQueryPort/TradingSchedulerCommandPort(Task 10 신설 — trigger only 2메서드, TradingSchedulerCommandHttpAdapter가 내부 API로 구현) 8개 — "port" NamedInterface로 공개(AdminUserViewPort는 AdminUserView와 함께 com.kista.user.application.port.output 소유)
  application/service/ ← internal(비공개) — AdminService/AdminQueryService/AdminReorderService/AdminStrategyService/AdminTradeCorrectionService/AdminPrivacyTradeService/RuntimeSettingsService 7개 (`AdminCycleCloser`/`AdminSelectionChain`은 Task 7에서 trading-core `com.kista.trading.application.service.CycleCloser`/`SelectionChain`으로 이관·개명됨 — admin 쪽엔 더 이상 존재하지 않는다). AdminQueryService는 Task 8부터 계좌 목록/단건 조회를 accountQueryPort(내부 API)로 위임하고, accountPort(trading-core AccountPort 직접 의존)는 getStats()의 countAll() 집계 한 곳에만 잔존
  adapter/in/web/     ← internal(비공개, AdminTradingSchedulerController 1개만 예외) — 컨트롤러 10개(AdminAccount/AdminDashboard/AdminObservability/AdminPing/AdminPrivacyTrade/AdminSettings/AdminTrade/AdminUser/RuntimeConfig/ClientErrorLog — KbLand 트리거를 겸하는 AdminScheduler는 `com.kista.web`, 위 "com.kista.web/" 참고) + AdminUserViews(응답 조립 헬퍼) + dto/ 21종
  adapter/in/web/AdminTradingSchedulerController ← Task 10 신설, `@RequestMapping("/api/admin/scheduler")` — POST `/open`·`/close` 2라우트(202, `scheduler.enabled` 게이팅 없음 — 내부 API 호출이라 kista-api role에서도 상시 노출). `com.kista.web.AdminSchedulerController`와 동일 prefix를 공유하지만 하위 경로가 겹치지 않아(그쪽은 `/kbland-*`만 남음) 라우팅 충돌 없이 공존 — `TradingSchedulerCommandPort`만 주입
  adapter/out/internal/ ← internal(비공개) — AccountQueryHttpAdapter(Task 8 신설, RestClient + `/api/internal/accounts` 호출)/PrivacyQueryHttpAdapter/TradingCommandHttpAdapter/TradingQueryHttpAdapter/TradingSchedulerCommandHttpAdapter(Task 10 신설, `internalApiRestClient`(짧은 타임아웃) + `/api/internal/trading/scheduler/{open,close}` 호출 — 트리거가 원격에서 가상 스레드만 띄우고 즉시 202를 반환하는 짧은 호출이라 reorder류의 `internalApiWriteRestClient`가 아닌 공용 빈 사용) — 전부 admin이 정의한 위 5개 포트를 내부 API(RestClient)로 구현
  adapter/out/persistence/audit/    ← internal(비공개) — AuditLogEntity/AppErrorLogEntity + *JpaRepository + *PersistenceAdapter 6파일
  adapter/out/persistence/settings/ ← internal(비공개) — RuntimeSettingsEntity + RuntimeSettingsJpaRepository + RuntimeSettingsPersistenceAdapter 3파일 (UserSettings*/UserNotificationPref*는 com.kista.user.adapter.out.persistence.settings 소유)
  ── `@Table(schema="public")`: AuditLogEntity/AppErrorLogEntity/RuntimeSettingsEntity 3개는 플랫폼 공통 테이블이라 public 스키마 명시 유지(`ddl-auto: validate` 무영향)

com.kista.user/     ← Spring Modulith 이전 모듈(CLOSED) — 가입·승인·프로필·설정 + JWT/RefreshToken/블랙리스트/카카오 OAuth 애그리게이트. "domain"(domain.model+domain.auth 병합)·"usecase"(application.usecase)·"port"(application.port.output)·"event"(application.event) 4개 NamedInterface 공개 — application.service·adapter.*·config는 의도적으로 비공개(모듈 내부 구현)
  domain/model/       ← User/UserSettings/AdminUserView — "domain" NamedInterface로 domain.auth와 병합 공개. `User.UserRole`/`UserStatus` nested enum + `NotificationType`은 여러 모듈 공유라 `com.kista.sharedkernel`(`@ApplicationModule(Type.OPEN)`) 독립 타입으로 이관(user는 소비만). `NotificationChannel`은 user 단독 소비라 `domain/model` 독립 파일로 잔류(DB `@Enumerated(STRING)` 상수명 byte-identical 유지). `User.DEFAULT_CHANNEL = NotificationChannel.NONE`은 domain 상수로 `User`에 그대로 유지
  domain/auth/        ← RefreshToken/TokenRefreshResult/TokenConstants/InvalidRefreshTokenException — domain/model과 함께 "domain" NamedInterface로 병합 공개
  application/usecase/ ← BlacklistUseCase/GetUserSettingsQuery/TokenUseCase/UpdateBalanceCheckUseCase/UpdateNotificationPrefUseCase/UpdateStrategySuggestionsUseCase/UserProfileUseCase/UserUseCase 8개 — "usecase" NamedInterface로 공개
  application/port/output/ ← AdminUserViewPort/ApprovalPolicyPort/BlacklistPort/KakaoOAuthPort/RefreshTokenPort/TelegramBotInfoPort/UserPort/UserSettingsPort 8개 — "port" NamedInterface로 공개. `ApprovalPolicyPort`(가입 승인 필요 여부 FOR UPDATE 락 조회)는 user가 admin의 `RuntimeSettingsPort`를 직접 참조하는 대신 필요한 만큼만 담은 전용 포트를 자체 정의하고 admin이 구현하는 포트 역전 패턴(broker `MockSimulationDataPort`와 동일 취지 → constraints.md "모듈 경계 own-type — 정당화 게이트")
  application/event/  ← user 모듈의 공개 계약 — NewUserRegisteredEvent/UserApprovedEvent/UserRejectedEvent/UserReappliedEvent 4개(`UserDeletedEvent`는 `com.kista.sharedkernel`로 승격 이전, 아래 sharedkernel 참고). notify 등 타 모듈이 `@TransactionalEventListener`로 구독(CLOSED↔CLOSED 모듈 간 이벤트 교차). "event" 이름으로 공개
  application/service/ ← internal(비공개) — BlacklistService/TokenService/UserCascadeDeleter/UserProfileService/UserService/UserSettingsService 6개. `UserCascadeDeleter`(`UserService.deleteMe`/`AdminService.deleteUser` 공통 cascade 삭제 진입점)는 trading(cyclePosition/strategyCycle)·finance(6포트+그룹승계)·account(계좌)·전략 설정(trading) cascade를 전부 `UserDeletedEvent` 발행으로 처리한다(직접 포트 호출 0, 각 모듈이 자체 리스너로 정리, EPR 재시도 보장). 계좌 cascade는 Task 12에서 동기 직접 호출 → `AccountUserCascadeListener`(AFTER_COMMIT) 비동기로 전환됐다 — 탈퇴 HTTP 응답 시점엔 계좌 소프트 삭제가 아직 미완료일 수 있다(최종적 일관성). `UserNotifyProfilePublisher`(package-private, 같은 패키지)는 상태·설정 변경 시 `UserNotifyProfileChangedEvent`를 발행하는 SSOT로, `UserService`의 상태 전이 5곳과 `UserSettingsService`의 알림/잔고검증 변경 2곳이 호출한다
  adapter/in/web/     ← internal(비공개) — AuthController/DevAuthController(local 전용)/SettingsController(user 셀프서비스) + dto/
  adapter/in/web/security/ ← internal(비공개) — JwtAuthFilter/InternalTokenAuthFilter/SecurityConfig/JwtDecoderConfig/JwtIssuerService/OpenApiConfig/RefreshTokenCookieHelper
  adapter/in/schedule/ ← internal(비공개) — RefreshTokenCleanupScheduler(platform `SchedulerJobRunner` 공통 골격 재사용)
  adapter/out/kakao/  ← internal(비공개) — KakaoOAuthAdapter/KakaoConfig/KakaoProperties
  adapter/out/redis/  ← internal(비공개) — RedisBlacklistAdapter
  adapter/out/persistence/user/    ← internal(비공개) — UserEntity + UserJpaRepository + UserPersistenceAdapter, AdminUserViewAdapter(`AdminUserViewPort` 구현)
  adapter/out/persistence/auth/    ← internal(비공개) — RefreshTokenEntity + RefreshTokenJpaRepository + RefreshTokenPersistenceAdapter
  adapter/out/persistence/settings/ ← internal(비공개) — UserSettingsJpaEntity/UserNotificationPrefJpaEntity/UserNotificationPrefId + *JpaRepository + UserSettingsPersistenceAdapter
  config/             ← internal(비공개) — AdminBootstrapProperties(`admin.kakao-ids` 환경변수 바인딩)/AdminConfig

com.kista.account/   ← Spring Modulith 이전 모듈(CLOSED) — 계좌 자격증명·브로커 연결 애그리게이트. "domain"(domain.model)·"usecase"(application.usecase)·"port"(application.port.output)·"event"(application.event) 4개 NamedInterface 공개 — application.service·adapter.*는 의도적으로 비공개(모듈 내부 구현)
  domain/model/       ← Account/RegisterAccountCommand/UpdateAccountCommand — "domain" NamedInterface로 공개. (`SellableQuantity`/브로커 자격증명 예외류는 account엔 없음 — broker 소유 동명 타입이 SSOT) `Account.Broker` nested enum 없음 — `com.kista.sharedkernel.Broker` 참조. `BrokerAccountRef`(broker)가 sharedkernel.Broker를 직접 참조한다. `AccountNumberMasker`는 순수 JDK 문자열 유틸이라(Account 자체에 의존 없음) `com.kista.sharedkernel`로 이관됨(Task 8 fix round 1) — root와 trading-core 양쪽이 이미 `:shared`에 의존하므로 계좌번호 마스킹이 필요한 어디서나 컴파일 의존 추가 없이 재사용 가능
  application/usecase/ ← AccountUseCase(조회/등록/수정/삭제/증권사 연결 테스트) — "usecase" NamedInterface로 공개
  application/port/output/ ← AccountPort 1개 — "port" NamedInterface로 공개. `BrokerEnabledPort`(`enabled(Broker): boolean`)는 account가 admin의 `RuntimeSettingsPort`를 직접 참조하는 대신 필요한 만큼만 담은 전용 포트로 자체 정의했던 것이나, root가 trading-core 정의 인터페이스를 구현할 수 없는 컴파일 경계 때문에 `com.kista.sharedkernel.port` 소유로 승격됐다(admin의 `RuntimeSettingsService`가 구현하는 포트 역전 패턴은 유지, user `ApprovalPolicyPort`와 동일) — account는 소비만 한다
  application/service/ ← AccountUserCascadeListener(sharedkernel `UserDeletedEvent` 구독 → `AccountPort.deleteByUserId` 소프트 삭제; `@TransactionalEventListener(AFTER_COMMIT, fallbackExecution=true)` + `@Transactional(REQUIRES_NEW)`) — user의 직접 포트 호출을 대체, trading/finance/strategy cascade와 동일 패턴
  application/event/  ← AccountDeletedEvent(UUID accountId) — account 모듈의 공개 계약. `AccountService.delete()`가 trading의 `StrategyPort.deleteByAccountId()`를 직접 호출하지 않고 이벤트로 발행하며, `com.kista.trading.application.service.AccountCascadeListener`가 `@TransactionalEventListener(AFTER_COMMIT)`로 구독(`UserDeletedEvent`+`UserCascadeListener`와 동일 메커니즘, EPR 추적)
  application/service/ ← internal(비공개) — AccountService(등록/수정/삭제/연결테스트, `BrokerConnectionTesters`/`BrokerEnabledPort` 소비)
  adapter/in/web/     ← internal(비공개) — AccountController + dto/(AccountRequest/AccountResponse/TestConnectionRequest) + AccountInternalController(`/api/internal/accounts`, Task 8 신설 — admin의 AccountQueryHttpAdapter가 소비하는 X-Internal-Token 전용 읽기 엔드포인트). trading Order/Strategy 내부 API와 달리 `Account`를 그대로 반환하지 않는다 — appKey/secretKey(복호화된 브로커 자격증명)가 내부망 응답에 실리는 것을 막기 위해 admin `AdminAccountView`와 byte-identical한 5필드 응답 own-type(`AccountInternalResponse`, 컨트롤러 내부 record)으로 매핑해 반환한다
  adapter/out/persistence/ ← internal(비공개) — AccountEntity + AccountJpaRepository + AccountPersistenceAdapter
  ── broker↔account 경계 규칙: broker는 `Account`를 전혀 참조하지 않는다 — 자체 소유 타입(`BrokerAccountRef`/`SellableQuantity`/`BrokerCredentialException`/`BrokerRateLimitException`, 위 "com.kista.broker/" 참고)만 사용한다. `Account→BrokerAccountRef` 변환은 `Account.toBrokerRef()` 1곳이 전담한다
  ── 통계 서비스 3종은 `com.kista.stats.application.service` 소속(위 "com.kista.stats/" 참고) — account는 forward 소비처가 아니다

com.kista.web/       ← Spring Modulith 앱셸 모듈. `@ApplicationModule(Type.CLOSED)` — NamedInterface **0개**. 아무 모듈도 web을 참조하지 않으므로 web이 무엇을 소비/구현하든 순환에 참여할 수 없다(sink) — 이 성질 덕에 다중 모듈 fan-out 컨트롤러뿐 아니라(아래 GlobalExceptionHandler/MetaController/AdminSchedulerController) 과거 순환 회피용으로 들어온 아웃바운드 포트 구현체 1개(`web/trading/ActiveStrategyCountAdapter`, 지금은 타입 의존 없는 순수 HTTP 어댑터라 위치만 잔재)도 이 모듈에 있다 — "pure inbound sink"는 더 이상 정확한 설명이 아니다. 나머지 1개였던 `TradingUserProfileAdapter`는 Task 12에서 삭제됐다(trading-core가 자기 복제본으로 자기 포트를 구현 — constraints.md "폐기된 전례" 참고). **신규 파일이 단일 모듈만 소비하거나 모듈 의존이 아예 없으면 web에 두지 말고 그 소유 모듈로 보낼 것** — 여기 남는 건 진짜 다중 모듈 fan-out과 순환 회피용 포트 구현체뿐(아래 ErrorLogAspect/MetricsConfig가 그 반대 사례)
  trading/             ← ActiveStrategyCountAdapter(`user.application.port.output.ActiveStrategyCountPort` 구현)는 :trading-core 컴파일 경계 제거 작업(Task 7)에서 trading-core 신규 내부 API(`com.kista.trading.adapter.in.web.ActiveStrategyCountInternalController`, `GET /api/internal/trading/active-strategy-count`)를 호출하는 순수 HTTP 어댑터로 축소됐다 — account/trading 타입 직접 참조가 없어져 순환 회피 근거는 더 이상 적용되지 않지만, 같은 포트 역전 계열이라 위치는 유지
  GlobalExceptionHandler ← root 소유 컨트롤러(admin/user/finance/stats/market/web) 전용 범용 예외(`SecurityException`→403/`NoSuchElementException`→404/`IllegalArgumentException`→400 등) → HTTP 코드 매핑(`@RestControllerAdvice`, `basePackages` 제약 없음). trading-core는 별도 프로세스(TradingApplication)라 이 advice와 같은 JVM에 공존하지 않으므로 fallthrough를 받지 않는다 — 동일한 범용 매핑을 trading-core 자체 `TradingExceptionHandler`의 `GENERIC_MAPPINGS`/`handleGeneric`이 독립적으로 복제 보유한다(위 "com.kista.trading/" TradingExceptionHandler 항목 참고). `BrokerCredentialException`/`BrokerRateLimitException`/`ManualTradingException`/`OrderCancelException`/`PrivacyTradeConflictException`/`Account.DuplicateAccountException`(broker/trading/privacy/account 소유, trading-core 네이티브 컨트롤러 전용)도 마찬가지로 `TradingExceptionHandler`(trading-core, `basePackages`로 스코핑 + `@Order(HIGHEST_PRECEDENCE)`)가 전담. `KisApiException`/`TossApiException`은 root/admin 소유 `AppErrorLogPort`(`saveErrorLog`) 의존 때문에 trading-core로 이관 불가해 root에 잔류(admin own-type `AdminBrokerCredentialException`/`AdminBrokerRateLimitException`과 함께) — `:trading-core → :api` 역방향 Gradle 의존 금지가 근거. 단일 모듈로 쪼갤 수 없는 진짜 앱 레벨 관심사(root 쪽)라 web 잔류
  MetaController        ← `GET /api/meta` — enum 메타(label/description) 단일 번들. finance(`domain.model`)+matching(`"kernel"`)+sharedkernel 3모듈 fan-out이라 단일 모듈 이관 불가, UI enum 리터럴 하드코딩 방지
  AdminSchedulerController ← stats "schedule" NamedInterface 소비 — KbLand 스케쥴러 3개 수동 트리거(`/api/admin/scheduler/kbland-*`). 2-role 배포에서 `kista-scheduler`에서만 유효 — 클래스 레벨 `@ConditionalOnProperty(scheduler.enabled)`로 kista-api role에선 빈 자체가 미등록(오라우팅 시 404). Task 10 이전엔 trading "schedule" NamedInterface(TradingOpenScheduler/TradingCloseScheduler)도 여기서 같이 소비했으나, root의 `:trading-core` 컴파일 경계 제거 작업으로 그 2개 트리거는 `com.kista.admin.adapter.in.web.AdminTradingSchedulerController`(내부 API 호출, 게이팅 없음)로 이관됐다 — 같은 `/api/admin/scheduler` prefix를 공유하되 하위 경로(`/open`,`/close` vs `/kbland-*`)가 겹치지 않아 두 컨트롤러가 라우팅 충돌 없이 공존한다. admin은 kista-api role에서도 상시 노출되는 모듈이라 role-게이팅된 이 컨트롤러(KbLand 몫)를 admin에 두면 "admin 컨트롤러는 항상 존재" 전제가 깨짐 — web 잔류가 여전히 정당
  dto/                 ← 4개 — enum 메타 전용 DTO(MetaBundle·EnumMeta·StrategyTypeMeta·TickerMeta). MetaController 소비 (사이클/전략 관련 DTO 10종은 `com.kista.trading.adapter.in.web.dto` 소유)
  ── `aop/ErrorLogAspect`(`NotifyPort.notifyError` AOP 오류 로깅 — admin 자체 포트 `AppErrorLogPort`만 컴파일 의존, notify는 포인트컷 문자열이라 의존 없음)는 `com.kista.admin.adapter.out.aop` 소유, `config/MetricsConfig`(모듈 의존 0의 순수 인프라)는 `com.kista.platform.metrics` 소유 — 둘 다 단일 모듈 의존/무의존이라 web이 아니라 각자 소유 모듈에 있다
  ── NOTE: `com.kista.web`은 `HexagonalArchitectureTest`의 `..adapter..`/`..application..`/`..domain..` 와일드카드 어디에도 걸리지 않는다(패키지에 그 세그먼트가 없음) — 나머지 레이어 규칙들의 검사 대상은 아니지만, `web_must_stay_pure_inbound_sink` 규칙이 `web → ..application.service../..adapter.out..` 의존을 직접 금지해 sink 전제를 강제한다(모듈 경계는 `ModulithArchitectureTest.verify()`가 별도로 policing)
```

### Spring Modulith 모듈 구성
15개 모듈(finance/notify/broker/trading/matching/market/marketcalendar/privacy/stats/admin/user/account/sharedkernel/platform/web — root `:api`·`:trading-core`·`:shared` 3개 Gradle 서브프로젝트에 나뉘어 위치) 전부 `@ApplicationModule`로 선언돼 있고, 모듈 간 경계는 `ApplicationModules.verify()`(`ModulithArchitectureTest`)가, 모듈 내부 레이어 방향은 `HexagonalArchitectureTest`가 각각 검증한다. 각 모듈이 공개하는 NamedInterface와 내부 패키지 구성은 위 패키지 트리에 모듈별로 기록돼 있다 — 신규 코드 추가 시 해당 모듈 절에서 위치·공개 범위를 확인할 것.

모듈 간 참조는 원칙적으로 상대 모듈이 공개한 NamedInterface(도메인 타입 또는 own-type projection)만 거쳐야 하며, 서로 참조가 얽히는 경우 포트 역전(own-type 정의 + 상대가 구현) 또는 이벤트 발행(`@TransactionalEventListener`, EPR로 재기동 시 재시도 보장) 패턴을 사용한다 — 각 모듈 절의 "own-type"/"이벤트" 설명이 현재 적용된 구체 사례다.

마이그레이션 상세 이력·순환 해소 경위는 `docs/agents/modulith-migration-history.md` 참고 (필요시 Read)

### 인증 userId 추출 패턴
- 모든 컨트롤러: `@AuthenticationPrincipal UUID userId` 메서드 파라미터로 직접 주입 — `SecurityContextHolder` 수동 호출 금지
- `JwtAuthFilter`: principal을 `UUID` 타입으로 저장 (`String` 아님)

### 소유권 검증 패턴
- `account.verifyOwnedBy(requesterId)` — 불일치 시 `SecurityException` (컨트롤러에서 403 매핑)
- `tradingCycle.verifyOwnedBy(account)` — `cycle.accountId().equals(account.id())` 검증, 마찬가지로 `SecurityException`
- 사이클 소유권 확인 순서: `cycleRepository.findByIdOrThrow(id)` → `accountRepository.findByIdOrThrow(cycle.accountId())` → `account.verifyOwnedBy(requesterId)`
- `accountRepository.findByIdOrThrow(id)` / `cycleRepository.findByIdOrThrow(id)` — 없으면 `NoSuchElementException` (컨트롤러에서 404 매핑)
- Service 내 반복 검증은 `private Account requireOwnedAccount(UUID accountId, UUID requesterId)` 헬퍼로 추출 — `AccountStatisticsService` 패턴 참고
- Controller에 try/catch 추가 금지 — `ResponseStatusException` 등 Spring HTTP 클래스는 application layer 사용 불가 (ArchUnit 규칙)

### JPA Auditing
- `BaseAuditEntity` (`@MappedSuperclass`): `UserEntity`, `AccountEntity`가 상속 — `@CreatedDate`/`@LastModifiedDate`로 `createdAt`/`updatedAt` 자동 관리
- 새 엔티티에 타임스탬프 필요 시 `BaseAuditEntity`(`createdAt`+`updatedAt`) 또는 `BaseCreatedAtEntity`(`createdAt`만) 상속 — `updated_at` 컬럼 없는 엔티티에 `BaseAuditEntity` 사용 금지 (`ddl-auto: validate` 실패); `com.kista.broker.adapter.out.persistence.KisTokenEntity` 등 DB DEFAULT(`insertable=false, updatable=false`) 방식 엔티티는 그대로 유지
- 서비스에서 domain record 생성 시: `updatedAt=null` (adapter가 무시, `@LastModifiedDate`가 처리), `createdAt`은 update 시 기존 값 보존 / register 시 `null` (`@CreatedDate`가 처리)
- `toEntity()` 내에서 `setCreatedAt()`/`setUpdatedAt()` 명시적 호출 금지 — `@CreatedDate(updatable=false)` / `@LastModifiedDate`가 INSERT·UPDATE 시 자동 처리. 호출 자체가 dead code이며 `@Setter(PACKAGE)` 범위 제약과도 충돌함

### 텔레그램 알림 (notifyTradingReport)
- 계좌별 텔레그램 설정 제거됨 — `User.telegramBotToken/chatId` 사용자봇만 사용 → 미설정 시 생략 (`log.warn`)
- `UserPersistenceAdapter`: telegramBotToken AES-256 암호화/복호화 적용

### BrokerAdapter Registry 패턴
- `com.kista.broker.application.service.BrokerAdapterRegistry`: `Map<Broker, BrokerAdapterPort>`(sharedkernel.Broker) — Spring이 `List<BrokerAdapterPort>` 자동 수집, broker 기준 Map 빌드
- `registry.require(account, XxxPort.class)` — 해당 브로커가 Capability 미지원 시 `IllegalArgumentException` → GlobalExceptionHandler 400
- `registry.find(account, XxxPort.class)` — `Optional<T>` 반환, 미지원 시 `Optional.empty()`
- 신규 브로커 추가: `com.kista.broker.application.port.output.BrokerAdapterPort` 구현체 1개만 추가 — Router/switch 수정 불필요
- `Account.isToss()` 삭제됨 — 브로커 분기 필요 시 `account.broker() == Broker.TOSS`(sharedkernel.Broker) 직접 비교
- `BrokerAdapterRegistry`는 `public` — 여러 모듈(account/trading/strategy)에서 "application" NamedInterface로 공개 소비하므로 예외적 공개 접근자

### TDA 전략 패턴 (InfiniteStrategy)
- `TradingService.execute()` 잔고 조회: KIS API 아님 → `findRecentByCycleId(cycleId, 1)` 최신 이력에서 `AccountBalance` 구성 (이력 없으면 `IllegalStateException`)
- PRIVACY execute() null guard: `snapshot=null` → `saveAndNotify`에서 `snapshot != null` 조건 가드 유지 필수
- **`preview()` today 오프셋**: 날짜 경계는 KST 04:30 (`DstInfo.SCHEDULER_RUN_TIME`) → `TradingPreviewService`의 `today = DstInfo.nextTradeDate()` SSOT 사용 (미적용 시 PRIVACY `findTodayTrade()` 날짜 1일 어긋남)
- **`INSUFFICIENT_BALANCE` skip 시 position 포함**: `shouldSkip(price)` true여도 `InfinitePosition`을 Result에 포함 — 프론트에서 단위금액·현재가·부족 금액 표시 목적

### CycleOrderStrategy Capability 패턴
- SSOT 위치: `com.kista.matching.domain.strategy`(커널 추출로 trading에서 이전, 위 "com.kista.matching/" 참고). `CycleOrderStrategy` 인터페이스: 전략 타입별 동작(basePrice 소스, 전일종가 필요 여부, 분할수, 리버스모드 지원, 청산 시 사이클 종료 여부, 최소시드, 예산배정 우선순위, compute skip, 롤오버 판정 필요 여부, BUY 가격 캡 보정 방식 등)을 캡슐화하는 다형성 계층 — 메서드별 상세는 코드가 SSOT
  - `canSkipOrderComputation()`은 기본 false이며 INFINITE만 complete concrete leg 또는 direction-aware legacy UNKNOWN 점유를 보수적으로 판단한다
  - `priceCapMode()`: VR도 생성 시점 cap을 적용하지 않고 접수 전 `BuyOrderPriceCapper`가 `VrStrategy.buildCappedBuyOrders()`로 보정한다
- `CycleOrderStrategies`: `Map<StrategyType, CycleOrderStrategy>`(`sharedkernel.StrategyType`) 라우터 — `of(type)` 으로 구현체 조회
- **프론트 capability 소비**: `GET /api/meta`의 `StrategyTypeMeta`에 capability 7필드(code/description/availableTickers/requiresPrivacyBase/tickerFixed/supportsReverseMode/divisionCounts) 직렬화 → 프론트는 `isInfinite` 휴리스틱 대신 `divisionCounts`/`requiresPrivacyBase` 직접 소비
- **최소시드 미리보기**: `GET /api/accounts/{id}/strategy-seed-preview?type=&ticker=&divisionCount=` → `StrategySeedPreviewResponse { ticker, basePrice, minSeed, skipReason }`
  - `StrategyService.strategySeedPreview()` 구현(trading) — `BrokerAdapterRegistry`(BrokerPricePort) + `PrivacyTradePort` + `CycleOrderStrategies.minRequiredDeposit` 조합. 브로커 HTTP 호출 포함이라 `@Transactional(NOT_SUPPORTED)` (register()와 동일)
  - PRIVACY + 기준 매매표 없는 날 → `skipReason="NO_PRIVACY_BASE"` (basePrice/minSeed=null)
- **신규 전략 타입 추가 시**: `Strategy.Type` enum case + `CycleOrderStrategy` 구현체 1개만 추가하면 메타 capability·최소시드·UI 자동 반영

### PRIVACY 전략 패턴 (기준 매매표)
- `privacy_trade_bases` (`com.kista.privacy.adapter.out.persistence`, `PrivacyTradeBaseEntity`): 전역 SSOT — 모든 PRIVACY 계좌가 공유, **account_id 없음** (계좌별 아닌 시스템 공통 기준)
  - `(release_date, ticker)` UNIQUE 제약 (`uq_privacy_trade_bases_release_date_ticker`) — 하루에 종목당 기준 매매표 1건
  - `updated_at` 없음 — `BaseCreatedAtEntity` 상속 (`createdAt`만)
- `privacy_trade_base_orders` (`com.kista.privacy.adapter.out.persistence`, `PrivacyTradeBaseOrderEntity`): 기준 매매표 1행에 대한 계획 주문 세트 (direction/orderType/quantity/price)
  - direction/orderType은 `sharedkernel.OrderDirection`{BUY/SELL} / `OrderType`{LOC/MOC/LIMIT}(상수명은 `Order.*`와 byte-identical) — VARCHAR + `@Enumerated(STRING)`
  - 저장 순서: **BUY → SELL**, BUY는 price **내림차순**, SELL은 price **오름차순** — `com.kista.privacy.adapter.out.persistence.PrivacyTradePersistenceAdapter` 정렬 처리
- FIDA 수신 흐름: `(tradeDate, ticker)` 없음 → 201 / 내용 동일 → 200(멱등) / 내용 다름 → `PrivacyTradeConflictException` → 409
- 스케쥴러: `StrategyType.PRIVACY` → `PrivacyCycleOrderStrategy.plan()` → `PrivacyStrategy.buildOrders()` (`CycleOrderComputer`가 전략별 분기)

### VR 전략 패턴 (밸류리밸런싱)
공식·bootstrap 규칙·가격 캡·롤오버 조건의 SSOT는 constraints.md "VR 공식" — 여기서는 구조·흐름만 기록.
- **TQQQ 전용** — `Strategy.Type.VR.resolveTicker()` → `Ticker.TQQQ` 강제. divisionCount 없음(null 직렬화), cycleSeedType=NONE 강제
- `strategy_vr_version` (`StrategyVrVersionEntity`): 전략 버전별 VR 설정 — intervalWeeks(롤오버 주기), bandWidth(밴드 폭 %), recurringAmount(USD, 양수=적립·0=거치·음수=인출) + 램프 파라미터 8개(initialGradient/gGraceWeeks/gStepWeeks/gMax/initialPoolLimitRate/pGraceWeeks/pStepWeeks/poolLimitFloor)
- `strategy_cycle_vr` (`StrategyCycleVrEntity`): 사이클 시작 시 스냅샷 — value(사이클 기준 V값), gradient(경과주수 기준 `gradientAt()` 재계산값), poolLimitRate(비율 스냅샷, 달러 아님 — poolLimit은 개장 `CyclePosition.usdDeposit×poolLimitRate`로 조회 시점 파생). `StrategyCycle.startAmount`는 모든 전략에서 개장 예수금+개장 보유분 시장가다.
- 주문 생성: `CycleOrderComputer` → `VrCycleOrderStrategy.plan()` → `VrStrategy.buildOrders()` → 매수·매도 사다리 LIMIT+AT_OPEN 주문 생성 (bootstrap은 LOC+AT_CLOSE)
- **holdings=0에도 사이클 유지** — `endsCycleOnLiquidation()=false`, `CyclePositionPersistor`가 종료 미발동
- **N주 롤오버**: `VrCycleRolloverService.rollIfDue()` — `CyclePositionPersistor`의 포지션 저장 직후 매일 판정, due이면 V′ 계산 후 기존 사이클 종료 + 새 사이클 원자 생성 (V′≤0 보류 등 규칙 → constraints.md). gradient·poolLimitRate는 스냅샷 이월이 아닌 전략 최초 사이클 startDate 기준 경과주수로 매번 재계산
- **운영 중 재설정**: `VrReconfigureService`(`com.kista.trading.application.service`, package-private) + `VrReconfigureUseCase` — `PUT /api/trading-cycles/{id}/vr-config`로 밴드폭·주기·적립금·램프 파라미터 수정 + 선택적 자본 주입/인출(수량/예수금)을 새 버전 발급+강제 롤오버 1회로 처리. 상세 규칙 → constraints.md "VR 공식"

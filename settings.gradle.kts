rootProject.name = "kista-api"

// 향후 멀티모듈 확장 시 아래 주석을 해제하고 모듈을 추가하세요
// include("starter-domain")
// include("starter-application")
// include("starter-adapter-web")
// include("starter-adapter-persistence")

// gradle/libs.versions.toml 은 Gradle 7.4+ 에서 자동으로 'libs' 카탈로그로 감지됩니다
// 별도 선언 없이 build.gradle.kts 에서 libs.* 로 바로 참조 가능합니다

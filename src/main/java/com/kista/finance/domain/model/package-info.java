// finance 모듈의 공개 계약 일부 — 불변 값 객체(record). "domain" 이름으로 공개된다(포트는 별도 "usecase"/"port" NamedInterface로 분리 공개).
@org.springframework.modulith.NamedInterface("domain")
package com.kista.finance.domain.model;
